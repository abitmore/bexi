bitshares:
    customer_account_id: 1.2.20138
    customer_account_active_key: "customer_account_active_key"
    customer_account_memo_key: "customer_account_memo_key"
        
    connection:
        Test:
         keys:  # needs both exchange and customer account active key since config is overwritten
            - "customer_account_memo_key" # customer account memo key
